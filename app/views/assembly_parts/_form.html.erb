<%= form_with(model: assembly_part) do |form| %>
  <% if assembly_part.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(assembly_part.errors.count, "error") %> prohibited this assembly_part from being saved:</h2>

      <ul>
        <% assembly_part.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :assembly_id, style: "display: block" %>
    <%= select(:assembly_part, :assembly_id  , Assembly.all.collect { |p| [ p.name, p.id ] }, { include_blank: true })%>
  </div>

  <div>
    <%= form.label :part_id, style: "display: block" %>
    <div style = "display:flex; flex-direction: column">
      <%= collection_check_boxes(:assembly_part, :part_id, Part.all, :id, :name, include_hidden: false ) %>
    </div>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
